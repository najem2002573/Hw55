package Selleniun_start_projects.Selleniun_start_projects;

public class isScalene {
	public boolean test(int a, int b, int c) {
        // A triangle is scalene if all sides are different
        return (a != b && b != c && a != c);
    }
}
